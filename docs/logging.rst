Logging strategy
================

For the whole package there is a a logging system.

On the console there is a logger running at the INFO level,
keeping track of what the code is doing. For the regression
a log file is saved inside the output folder with some summary
statistics of the results.
